<head>
</head>
<div style="height:30px;">
</div>
<div class="container" style="background-color:transparent !important;">
  <% if !author_signed_in? %>
    <div class="jumbotron">
    <h1>What is MadScript?</h1>
    <p>A CRUD application to stimulate a collaboration between writers. Users can sign up for FREE. If a user has yet to sign up, then the user is only allowed to see the scripts therefore missing out on taking part of great community involvement.</p>
    <p>A user, also known as an author, can start his/her own <b><i>Script!</i></b> The author can also contribute to the work of others by continually adding <b><i>Stanzas</i></b> to any script created by other authors.</p>
    <p>Below, you can see some of the <b><i>Scripts</i></b> community authors have already contributed to! Don't wait any longer, join us!</p>
      <p class="text-center">
        <%= link_to "Start Writing Now", new_author_registration_path, class: "btn btn-lg btn-info" %>
      </p>
    </div>
  <% end %>
    <div class="row">
      <div class="col-lg-9 col-md-9 col-sm-12 col-xs-12">
        <ul class="nav nav-tabs" id="hometabs">
          <li class="active"><a href="#hot" data-toggle="tab">Trending</a></li>
          <li><a href="#new" data-toggle="tab">New</a></li>
          <% if author_signed_in? %>
            <li><a href="#groups" data-toggle="tab">Groups</a></li>
          <% end %>
        </ul>
        <div class="tab-content" id="content">
          <div class="tab-pane fade in active" id="hot">
            <br>
              <table class="table table-hover">
                <thead>
                  <tr>
                    <th>Author</th>
                    <th>Title</th>
                    <th>Genre</th>
                    <th>Stanzas</th>
                    <th>Votes</th>
                  </tr>
                </thead>
                <tbody>
                  <% Script.find_each do |script| %>
                    <tr class="clickableRow" href="../scripts/<%= script.id %>" style="cursor:pointer;">
                      <td><%= script.author.username %></td>
                      <td><%= script.title %></td>
                      <td><%= script.genre %></td>
                      <td><%= script.num_of_stanzas %></td>
                      <td><%= script.votes %></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          <div class="tab-pane fade" id="new">
            <h4>Most recent <b><i>Scripts</i></b> will be displayed.</h4>
          </div>
          <div class="tab-pane fade" id="groups">
            <h4>Feature coming soon.</h4>
            <p>If the <i>Author</i> is part of a <i>Group</i> then there will be a list of <i>Groups</i> the <i>Author</i> belongs in.</p>
            <p>Else, there will be a section showcasing <i>Groups</i> the <i>Author</i> might be interested in!</p>
          </div>
        </div>
      </div>
    <% if author_signed_in? %>
      <div class="col-lg-3 col-md-3 col-sm-6 col-xs-12">
        <div class="panel panel-info">
          <div class="panel-heading">
            <h3 class="panel-title text-center">Start writing, <%= current_author.username %>!</h3>
          </div>
          <div class="panel-body">
            <p>
              <div class="input-group">
                <div class="input-group-btn">
                  <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">Search <span class="caret"></span></button>
                  <ul class="dropdown-menu" role="menu">
                    <li><a href="#"><i>Author</i></a></li>
                    <li><a href="#"><i>Script</i></a></li>
                    <li><a href="#"><i>Group</i></a></li>
                    <li class="divider"></li>
                    <li><a href="#">Partners</a></li>
                  </ul>
                </div><!-- /btn-group -->
                <input type="text" class="form-control" placeholder="Coming soon...">
              </div><!-- /input-group -->
            </p>
            <hr>
            <p><%= button_to "Author a new Script!", new_script_path, :class => "btn btn-warning", :method => :get, :style => "width:100%" %></p>
            <p><button style="width:100%" type="button" class="btn btn-primary">Host a new <i>Group</i>!</button></p>
          </div>
        </div>
      </div>
    </div>
<hr />
  </div>
  <% end %>
</div>

<script type="text/javascript">
  jQuery(document).ready(function($) {
        $(".clickableRow").click(function() {
              window.document.location = $(this).attr("href");
        });
  });
</script>
